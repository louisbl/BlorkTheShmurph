package crm.gobelins.darkunicorn.signals
{
	import org.osflash.signals.Signal;
	
	public class GameRestartSignal extends Signal
	{
		public function GameRestartSignal(...parameters)
		{
			super(parameters);
		}
	}
}