package crm.gobelins.darkunicorn.signals
{
	import org.osflash.signals.Signal;
	
	public class GameResumeSignal extends Signal
	{
		public function GameResumeSignal(...parameters)
		{
			super(parameters);
		}
	}
}